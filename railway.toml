[build]
builder = "nixpacks"
watchPatterns = [
  "/backend/**",
  "/railway.toml"
]

[deploy]
startCommand = "uvicorn server:app --host 0.0.0.0 --port $PORT"
restartPolicyType = "on_failure"

[[services]]
name = "backend"
source = "backend"
